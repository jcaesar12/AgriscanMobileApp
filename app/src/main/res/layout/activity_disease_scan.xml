<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true"
            android:background="#e9e9e9">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

        <!-- 1. Top Gradient Card -->
        <include layout="@layout/card_disease_scanner_header" />

        <!-- 1b. How to use the scanner -->
        <include layout="@layout/card_disease_scanner_tips" />

        <!-- 2. Two Action Cards: Take Photo + Upload Image -->
        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="16dp">

            <include layout="@layout/card_action_take_photo" />
            <include layout="@layout/card_action_upload_image" />
        </LinearLayout>

        <!-- 3. Recent Scans -->
        <include layout="@layout/card_recent_scans" />

        <!-- 4. Image Preview (Initially GONE, shown after taking/uploading image) -->
        <include layout="@layout/card_image_preview" />

        <!-- 5. Loading State (Initially GONE, shown when analyzing) -->
        <include layout="@layout/card_loading_analyze" />

    </LinearLayout>
</ScrollView>
